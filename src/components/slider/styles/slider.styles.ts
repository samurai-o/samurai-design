import {makeStyles } from "../../styles";

export default makeStyles({
    sliderContainer: {
        width: '100%'
    },
    sliderTrace: {
        width: '100%',
        position: 'relative',
        top: 0,
        left: 0,
        overflow: "hidden"
    }
});